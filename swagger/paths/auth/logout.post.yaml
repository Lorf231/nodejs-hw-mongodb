tags:
  - Auth
summary: Logout user
operationId: logoutUser
description: Видаляє поточну сесію користувача.
responses:
  '204':
    description: Successfully logged out (no content)
  '401':
    description: Invalid or missing token
    content:
      application/json:
        schema:
          $ref: ../../components/schemas/Error.yaml